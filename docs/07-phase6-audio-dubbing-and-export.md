# 07 – Phase 6: Audio Dubbing & Export

Goal:
- AudioConfig per clip: BGM, volume, voice provider.
- ExportJob untuk proses ffmpeg.

Endpoint (TODO):
- POST /api/viral-clip/clips/{id}/exports
- GET  /api/exports/{id}
